# SUMMIT 리팩토링 — 가설 검증 및 방향 수정 기록

> 목적: 면접 준비 시 "왜 이 판단을 했는가"를 다시 설명할 수 있도록, 최초 가설이 실제 진단 결과로 어떻게 수정됐는지 시간순으로 기록.

---

## 1. 최초 가설 (2026-08-31 이전)

**가설**: "사용자 피드백(로딩 느림, 카드 로딩 지연)의 원인은 `public` 폴더에 이미지를 최적화 없이 그대로 넣어둔 것이다."

**근거**: 사용자 코멘트("로딩이 느리다", "카드/이미지 로딩이 오래 걸린다")와 본인이 알고 있던 사실("public 폴더에 사진을 다 때려박음")을 결합한 추론. 코드나 수치를 직접 확인하기 전 단계.

**당시 세운 해결 계획**: `public` 폴더 이미지를 S3로 마이그레이션 + 최적화. 이걸 SUMMIT 리팩토링의 메인 스토리로 설정.

---

## 2. 검증 절차

가설을 그대로 실행하지 않고, 먼저 진단 도구로 확인하는 순서를 택함:

1. Chrome DevTools Lighthouse로 Before 지표 측정 (Mobile, Navigation 모드, 시크릿 모드로 확장 프로그램 영향 배제)
2. 3개 주요 페이지(`/`, `/setlist`, `/event-goods`) 측정
3. Network 탭 워터폴로 실제 어떤 리소스가 병목인지 직접 확인

**측정 결과**:

| 페이지         | Performance | LCP      | Total Byte Weight |
| -------------- | ----------- | -------- | ----------------- |
| `/`            | 78          | 4.4s     | 974 KiB           |
| `/setlist`     | 79          | 4.8s     | 784 KiB           |
| `/event-goods` | 71          | **9.8s** | **2,387 KiB**     |

`/event-goods`가 압도적으로 심각했고, 이미지 개수(18개)가 가장 많은 페이지였음.

---

## 3. 가설과 실제 원인의 차이 (핵심 발견)

Network 탭으로 `/event-goods`의 무거운 리소스를 직접 확인한 결과:

- 문제의 이미지 18개는 **`public` 폴더가 아니라 외부 CDN(Apple Music, `mzstatic.com`)에서 직접 가져오는 앨범 커버**였음
- 전부 `600x600bb.jpg` — 원본 해상도 그대로 요청 (개당 60~190KB, 합계 약 1.9MB)
- 실제 화면에서는 40~60px 크기의 작은 썸네일로만 쓰임 → 필요한 크기의 수십 배를 그대로 받아오고 있었음

**즉, 최초 가설("public 폴더가 원인")은 절반만 맞았음**:

- `public` 폴더 이미지 문제는 실재함 (홈/setlist, 총 3.6MB, `next/image` 미적용 확인됨)
- 하지만 **가장 심각한 병목(LCP 9.8초)의 실제 원인은 별도 소스(외부 CDN 원본 이미지)**였고, 최초 가설엔 없던 원인이었음

---

## 4. 판단 수정

| 항목             | 최초 계획                            | 수정된 계획                                                                                                                                                                            | 수정 이유                                                                                                                                   |
| ---------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 메인 해결책      | public → S3 마이그레이션             | 외부 CDN 이미지 리사이징/프록시 (최우선) + public → next/image 전환 (2순위)                                                                                                            | LCP 최대 병목의 실제 소스가 S3와 무관한 외부 CDN이었기 때문. S3 이전만 했다면 가장 심각한 문제(9.8초)는 그대로 남았을 것                    |
| 스크롤 끊김 원인 | (미확정, 이미지 문제로 추정)         | CLS는 0으로 측정되어 레이아웃 시프트는 원인이 아님 확인. 실제 원인은 이미지 디코딩 비용 또는 framer-motion 애니메이션의 메인 스레드 점유로 추정 — Performance 탭 녹화로 추가 검증 필요 | Lighthouse의 CLS는 로드 시점만 측정하고 로드 후 스크롤 중 버벅임은 못 잡는다는 것을 확인                                                    |
| Analytics        | 리팩토링 이후 여유 있을 때 설치 예정 | 최우선 순위로 격상                                                                                                                                                                     | 1학기에 analytics 자체가 없었다는 사실을 재확인 — Before/After 성능 지표는 있어도 "실사용자에게 미친 영향"을 보여줄 데이터가 없다는 갭 발견 |

---

## 5. 이 과정에서 얻은 판단 원칙 (면접 답변 포인트)

- **추측을 실행하지 않고 먼저 계측**: "public 폴더가 문제일 것"이라는 그럴듯한 가설이 있었지만, 바로 마이그레이션에 착수하지 않고 Lighthouse + Network 탭으로 먼저 검증함
- **가설이 틀렸을 때 빠르게 인정하고 방향 수정**: 최초 계획(S3 마이그레이션)을 고집하지 않고, 데이터가 가리키는 실제 병목(외부 CDN 원본 이미지)으로 우선순위를 재조정함
- **지표의 한계를 이해하고 해석**: CLS=0을 "레이아웃 시프트 없음"으로 정확히 해석하되, "그래서 스크롤 끊김과 무관하다"고 성급히 결론 내리지 않고 다른 원인(디코딩 비용, 애니메이션)을 추가로 검토함
- **사후 대응이 아니라 재발 방지까지 고려**: 1학기엔 analytics가 없어서 실사용자 영향을 증명 못 한다는 갭을 인지하고, 다음 실사용 시점(12월 정기공연) 전에 선제적으로 설치하기로 결정

---

## 6. 참고 — 원본 진단 기록

- 상세 수치, 발견된 문제 8가지 전체 목록: `REFACTOR_NOTES.md` (2026-08-31 작성)
- Lighthouse 원본 리포트: `lighthouse-before-{home,setlist,event}-0831` (HTML/JSON)
