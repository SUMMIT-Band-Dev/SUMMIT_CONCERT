# dependencies
node_modules

# build output
dist

# env (실제 값은 절대 커밋 금지, .env.example만 커밋)
.env
.env.*
!.env.example

# Prisma 생성 클라이언트 (prisma generate로 재생성됨)
src/generated

# logs / caches
*.log
*.tsbuildinfo
coverage
